"use client";

import { gsap } from "gsap";

if (typeof window !== "undefined") {
  // Register any GSAP plugins here
  // We use core gsap.timeline only — no plugins needed for this sequence
}

export { gsap };
